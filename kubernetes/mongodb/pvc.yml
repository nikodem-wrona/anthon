apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: anthon-mongodb-pvc
  namespace: anthon-prod
spec:
  storageClassName: manual
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 10Gi